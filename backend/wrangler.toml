name = "epoch-worker"
main = "build/worker/shim.mjs"
compatibility_date = "2024-01-01"

[build]
command = "worker-build --release"

# Deploy: `wrangler deploy --env staging` (see .github/workflows/deploy-staging.yml)
[env.staging]
name = "epoch-worker-staging"
